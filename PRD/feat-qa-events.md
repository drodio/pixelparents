## Progress Update as of [June 30, 2026 — 9:53 PM Pacific]

### Summary of changes since last update
First entry for `feat/qa-events`. Fixed the 4 verified QA findings for the Events surface (calendar list/this-week classification, "+N more" dead click, silent end-date drop, wrong signed-out area label), staying strictly within the owned files (`app/(authed)/events/**`, `lib/events/**`, `lib/db/events.ts`). Respected the existing timezone model: all-day = UTC calendar day rendered via UTC getters; today/current-month from the client's local clock.

### Detail of changes made:
- **Finding 1 (HIGH) — all-day today events wrongly classified as Past** — `lib/events/calendar.ts`. `splitUpcomingPast` and `eventsThisWeek` compared the raw UTC-midnight instant of all-day events against `now`, so a today-dated all-day event (e.g. "First Day of Class" 8/19 stored at 8/19 00:00 UTC) was "past" for most of the local day for any viewer west of UTC. Added `allDayNotPast(ev, now)` helper that compares the event's inclusive UTC end-day (`utcDayKey`) against today's LOCAL day key (`localDayKey`). `splitUpcomingPast` now routes all-day events through it; timed events keep the instant comparison. `eventsThisWeek` gained a parallel all-day branch: UTC-day-range overlap against the window's day keys (today .. today+6 inclusive), timed events keep the `[today, +7d)` instant overlap. Reuses existing `utcDayKey`/`localDayKey` exactly like `eventOverlapsDay`. No regression to the placement/grid path.
- **Finding 2 (MEDIUM) — "+N more" was a dead click** — `app/(authed)/events/events-calendar-client.tsx`. The overflow label was a `pointer-events-none` span; clicks fell through to the opaque z-10 column that has no handler and blocks the z-0 pick-day button. Replaced the span with a `<button type="button" onClick={() => onPickDay(key)}>` (amber focus ring, hover lightening). Pills keep their own `onPickEvent` handlers; no nested-button issue since it's a sibling, not a wrapper.
- **Finding 3 (LOW) — end silently dropped when "Add an end" checked but date blank** — `app/(authed)/events/actions.ts` `validateEventForm`. The form sends `endDate: null` when no end is requested, but `""` (a string) when the user checked the box and left the date blank. Previously `input.endDate ? … : null` coerced `""` to "no end" silently. Now we distinguish `endRequested = endDate !== null/undefined`; if an end was requested but `resolveInstant` returns null (blank/unparseable), we return a clear error ("Add an end date, or uncheck it." / "...date and time..." for timed) instead of saving with no end. Server-side so it covers both create + edit. The edit page seeds `endDate` from stored value and `hasEnd = Boolean(initial?.endDate)`, so an event with no end still sends `null` — no false positives.
- **Finding 4 (LOW) — signed-out Events pages said "access your community"** — `app/(authed)/events/page.tsx` and `app/(authed)/events/gate.tsx` now pass `area="events"` to `SignedOutPanel`. The `directory/page.tsx` half of the finding is OUT OF SCOPE (not an owned file) and was intentionally left untouched.

### Tests
- Added UTC-fixture tests to `lib/events/calendar.test.ts`: `splitUpcomingPast` keeps a today-dated all-day event upcoming (8am local == event UTC day), classifies yesterday-dated all-day as past, keeps a multi-day all-day upcoming through its inclusive UTC end day; `eventsThisWeek` includes today-dated all-day, respects the 7th/8th day boundary, excludes yesterday. All 66 `lib/events` tests pass. `npm run typecheck` clean; eslint clean on all touched files.
- Finding 3 has no unit test: `validateEventForm` is an un-exported helper inside a `"use server"` module that imports Clerk/DB; isolating it would require restructuring the server-action module. The change is a narrow null-vs-blank distinction (not date math), so verified by reading the create/edit payload contract rather than a fixture.

### Potential concerns to address:
- Did NOT run `next build` in the worktree (per directive). Note for reviewer.
- Finding 4's directory fix (`app/(authed)/directory/page.tsx`, `area="directory"`) is left for a directory-owning change — flagged, not fixed, to respect file ownership.
