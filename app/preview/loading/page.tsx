import { LoadingScreen } from "@/components/screens/loading-screen";

export const metadata = { title: "Loading — preview", robots: { index: false } };

export default function LoadingPreview() {
  return <LoadingScreen />;
}
