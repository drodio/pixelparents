import { NotFoundScreen } from "@/components/screens/not-found-screen";

export const metadata = { title: "404 — preview", robots: { index: false } };

export default function NotFoundPreview() {
  return <NotFoundScreen />;
}
