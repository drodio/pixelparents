// Root Suspense fallback — shown while a route segment streams in (e.g. a slow
// Neon cold start). See components/screens/loading-screen.tsx.
export { default } from "@/components/screens/loading-screen";
